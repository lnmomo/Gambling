import type {KellyStakeOutput} from "../types";
const safe = (value: number) => Number.isFinite(value) ? value : 0;
export function calculateKellyStake(probability:number,odds:number,bankroll:number,options:{kellyFraction?:number;maxStakePct?:number}={}):KellyStakeOutput{const warnings:string[]=[];const p=safe(probability),o=safe(odds),bank=safe(bankroll),fraction=Math.min(options.kellyFraction??.25,.5),maxPct=options.maxStakePct??.01;if(p<=0||p>=1||o<=1||bank<=0){warnings.push("Invalid Kelly input; stake set to 0.");return{probability:p,odds:o,edge:0,fullKellyFraction:0,fractionalKellyFraction:0,rawStake:0,cappedStake:0,positiveKelly:false,warnings}}const edge=p*o-1,full=Math.max(0,edge/(o-1));if(full<=0)warnings.push("Kelly fraction <= 0，当前方向不具备正 Kelly 价值。");const fractional=full*fraction,raw=bank*fractional,maxStake=bank*maxPct,capped=Math.max(0,Math.min(raw,maxStake));if(raw>capped)warnings.push("Stake capped by max single bet limit.");return{probability:p,odds:o,edge,fullKellyFraction:full,fractionalKellyFraction:fractional,rawStake:raw,cappedStake:capped,positiveKelly:full>0,warnings}}
