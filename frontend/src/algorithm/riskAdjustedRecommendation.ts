import type {BankrollConfig,BankrollTransaction,MatchPrediction} from "../types";
import {calculateStakeRecommendation} from "./bankrollManager";
import {calculateCorrelationRisk} from "./correlationRisk";
import {calculateDrawdownState} from "./drawdownControl";
import {calculatePortfolioExposure} from "./portfolioRisk";
export function attachStakeRecommendationToPrediction(prediction:MatchPrediction,bankrollConfig:BankrollConfig,activePredictions:MatchPrediction[]=[],transactions:BankrollTransaction[]=[],date=new Date().toISOString().slice(0,10)):MatchPrediction{const drawdownState=calculateDrawdownState(transactions,bankrollConfig),correlationRisk=calculateCorrelationRisk(prediction,activePredictions),portfolio=calculatePortfolioExposure(activePredictions,bankrollConfig.currentBankroll,date),stakeRecommendation=calculateStakeRecommendation(prediction,bankrollConfig,portfolio,{drawdownState,correlationRisk});const blocked=stakeRecommendation.status==="STAKE_BLOCKED";return{...prediction,stakeRecommendation,suggestedStake:stakeRecommendation.finalStake,stakeFraction:stakeRecommendation.stakePctOfBankroll,lifecycleStatus:blocked?"NO_BET":prediction.lifecycleStatus,recommendation:blocked?"NO_BET":prediction.recommendation,criticReport:blocked?{...prediction.criticReport,passed:false,finalAction:"NO_BET",reasons:[...prediction.criticReport.reasons,...stakeRecommendation.reasons]}:prediction.criticReport}}
