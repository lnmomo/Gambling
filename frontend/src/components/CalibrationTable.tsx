import type {CalibrationBucket} from "../types";
export default function CalibrationTable({rows}: {rows: CalibrationBucket[]}) { return <section className="panel"><div className="panel-heading"><div><h2>概率校准表</h2><p>预测概率应长期接近真实命中率</p></div></div><div className="table-scroll"><table className="data-table"><thead><tr><th>概率区间</th><th>样本数</th><th>平均预测概率</th><th>真实命中率</th><th>校准误差</th></tr></thead><tbody>{rows.map(row => <tr key={row.bucket}><td>{row.bucket}</td><td>{row.count}</td><td>{(row.avgPredictedProbability * 100).toFixed(1)}%</td><td>{(row.actualHitRate * 100).toFixed(1)}%</td><td className={row.calibrationError > .15 ? "negative" : row.calibrationError > .08 ? "warning-text" : "positive"}>{(row.calibrationError * 100).toFixed(1)}%</td></tr>)}</tbody></table></div></section>; }
