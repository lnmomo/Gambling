import type {BacktestRecord} from "../types";
import {analyzeClv} from "../algorithm/clvAnalysis";
export default function ClvPanel({records}: {records: BacktestRecord[]}) { const report = analyzeClv(records); return <section className="panel"><div className="panel-heading"><div><h2>CLV 分析</h2><p>推荐时官方 SP 与收盘 SP 的价格优势</p></div></div><div className="summary-strip backtest-inner"><span>平均 CLV<b>{(report.averageClv * 100).toFixed(2)}%</b></span><span>中位 CLV<b>{(report.medianClv * 100).toFixed(2)}%</b></span><span>正 CLV 比例<b>{(report.positiveClvRate * 100).toFixed(1)}%</b></span></div><div className="table-scroll"><table className="data-table"><thead><tr><th>联赛</th><th>样本</th><th>平均 CLV</th><th>正 CLV 比例</th></tr></thead><tbody>{report.clvByLeague.map(row => <tr key={row.groupName}><td>{row.groupName}</td><td>{row.count}</td><td>{(row.metrics.averageClv * 100).toFixed(2)}%</td><td>{(row.metrics.positiveClvRate * 100).toFixed(1)}%</td></tr>)}</tbody></table></div></section>; }
