import type {BacktestRecord} from "../types";
export default function EquityCurvePanel({records}:{records:BacktestRecord[]}){const rows=records.filter(row=>row.bankrollAfter!==undefined);return <section className="panel"><h2>Bankroll Curve</h2><div className="table-scroll"><table className="data-table"><thead><tr><th>时间</th><th>赛前资金</th><th>赛后资金</th><th>Profit</th></tr></thead><tbody>{rows.slice(0,20).map(row=><tr key={row.matchId}><td>{new Date(row.kickoffTime).toLocaleDateString("zh-CN")}</td><td>{row.bankrollBefore?.toFixed(2)}</td><td>{row.bankrollAfter?.toFixed(2)}</td><td>{row.profit.toFixed(2)}</td></tr>)}</tbody></table></div></section>}
