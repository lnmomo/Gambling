import type {ErrorAnalysisReport} from "../types";
export default function ErrorAnalysisPanel({report}: {report: ErrorAnalysisReport}) { return <section className="panel"><div className="panel-heading"><div><h2>错误诊断</h2><p>按联赛、推荐类型、EV 和风险维度定位系统性偏差</p></div></div><div className="backtest-diagnostics">{report.summary.map(text => <p key={text}>{text}</p>)}</div><div className="two-column backtest-inner"><div><h3>常见 No Bet 原因</h3>{report.commonNoBetReasons.slice(0, 6).map(item => <p key={item.reason}>{item.reason} <b>{item.count}</b></p>)}</div><div><h3>常见 Warning</h3>{report.commonWarnings.slice(0, 6).map(item => <p key={item.warning}>{item.warning} <b>{item.count}</b></p>)}</div></div></section>; }
