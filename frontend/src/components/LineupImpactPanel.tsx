import type{LineupImpactOutput}from"../types";export default function LineupImpactPanel({impact}:{impact:LineupImpactOutput}){return <div><h3>阵容/伤停影响</h3><div className="summary-strip backtest-inner"><span>主队进攻 / 防守<b>{impact.homeAttackFactor.toFixed(3)} / {impact.homeDefenseFactor.toFixed(3)}</b></span><span>客队进攻 / 防守<b>{impact.awayAttackFactor.toFixed(3)} / {impact.awayDefenseFactor.toFixed(3)}</b></span><span>门将风险 主 / 客<b>{impact.homeGoalkeeperRisk.toFixed(3)} / {impact.awayGoalkeeperRisk.toFixed(3)}</b></span><span>风险等级<b>{impact.riskLevel}</b></span></div>{impact.appliedEvents.map(e=><p key={`${e.eventId}-${e.factorType}`}>{e.description} · {e.factorType}: {(e.factorChange*100).toFixed(1)}%</p>)}</div>}
