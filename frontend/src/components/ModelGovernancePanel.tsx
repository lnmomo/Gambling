import {evaluateModelPromotion} from "../algorithm/modelGovernance";
import {challengerModel, championModel} from "../data/mockModelRegistry";
import ModelVersionBadge from "./ModelVersionBadge";
export default function ModelGovernancePanel() { const decision = evaluateModelPromotion(challengerModel, championModel); return <section className="panel"><div className="panel-heading"><div><h2>模型治理</h2><p>挑战者只接受离线评估，不会自动替换生产冠军。</p></div><ModelVersionBadge source={decision.decision}/></div><div className="summary-strip"><span>冠军模型<b>{championModel.version}</b></span><span>挑战者<b>{challengerModel.version}</b></span><span>测试样本<b>{challengerModel.testMatchCount}</b></span><span>结论<b>{decision.decision}</b></span></div><div className="table-scroll"><table className="data-table"><thead><tr><th>条件</th><th>当前值</th><th>阈值</th><th>结果</th></tr></thead><tbody>{decision.requiredConditions.map(row => <tr key={row.name}><td>{row.name}</td><td>{String(row.value)}</td><td>{String(row.threshold)}</td><td>{row.passed ? "通过" : "未通过"}</td></tr>)}</tbody></table></div></section>; }
