import type {PortfolioExposure} from "../types";
const pct=(value:number)=>Number.isFinite(value)?`${(value*100).toFixed(2)}%`:"-";
export default function PortfolioExposurePanel({exposure}:{exposure:PortfolioExposure}){return <section className="panel"><div className="panel-heading"><div><h2>组合风险暴露</h2><p>{exposure.date} · ACTIVE {exposure.activeRecommendationCount} 场</p></div></div><div className="summary-strip" style={{padding:16,margin:0}}><span>总 Stake<b>{exposure.totalStake.toFixed(2)}</b></span><span>Bankroll 占比<b>{pct(exposure.totalStakePct)}</b></span><span>单场最大<b>{exposure.maxSingleBetStake.toFixed(2)}</b></span><span>日限额使用<b>{pct(exposure.dailyLimitUsedPct)}</b></span></div><div className="table-scroll"><table className="data-table"><thead><tr><th>联赛</th><th>Stake</th><th>占比</th><th>场数</th></tr></thead><tbody>{exposure.exposureByLeague.map(row=><tr key={row.league}><td>{row.league}</td><td>{row.stake.toFixed(2)}</td><td>{pct(row.stakePct)}</td><td>{row.recommendationCount}</td></tr>)}</tbody></table></div>{exposure.warnings.map(text=><p key={text}>{text}</p>)}</section>}
