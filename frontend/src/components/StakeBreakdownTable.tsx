import type {BacktestRecord} from "../types";
export default function StakeBreakdownTable({records}:{records:BacktestRecord[]}){const groups=[...new Set(records.map(row=>row.stakeStatus??"UNKNOWN"))];return <section className="panel"><h2>Stake Distribution</h2><div className="table-scroll"><table className="data-table"><thead><tr><th>Status</th><th>Count</th><th>Total Stake</th><th>Profit</th></tr></thead><tbody>{groups.map(group=>{const rows=records.filter(row=>(row.stakeStatus??"UNKNOWN")===group);return <tr key={group}><td>{group}</td><td>{rows.length}</td><td>{rows.reduce((s,r)=>s+r.stake,0).toFixed(2)}</td><td>{rows.reduce((s,r)=>s+r.profit,0).toFixed(2)}</td></tr>})}</tbody></table></div></section>}
