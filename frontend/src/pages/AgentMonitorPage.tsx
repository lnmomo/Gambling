import PageHeader from "../components/PageHeader";
import useOfficialMatches from "../hooks/useOfficialMatches";
export default function AgentMonitorPage() { const {matches, loading, error} = useOfficialMatches(); const blocked = matches.filter(match => !match.prediction.criticReport.passed); return <div className="page"><PageHeader title="Agent / Workflow 监控" subtitle="Critic Agent 展示内容与 criticRules 的真实输入、输出和硬规则保持一致" />{loading ? <p className="empty-state">加载中...</p> : error ? <p className="empty-state">{error}</p> : <><section className="panel workflow-panel"><div className="panel-heading"><div><h2>统一决策链路</h2><p>官方比赛池 → SP 隐含概率 → Dixon-Coles → Elo → 上下文修正 → 融合 → Critic → 推荐 / NO_BET</p></div></div><div className="workflow-graph">{["官方数据", "特征构建", "概率模型", "模型融合", "Critic Agent", "资金管理"].map((name, index, list) => <div className="workflow-segment" key={name}><div className={`workflow-node ${name.includes("Critic") ? "critic" : ""}`}><i>✓</i><b>{name}</b><small>确定性规则</small></div>{index < list.length - 1 && <span className="workflow-arrow">→</span>}</div>)}</div></section><div className="two-column" style={{marginTop: 16}}><section className="panel rules-card"><h2>Critic 关键规则</h2><p>无官方比赛 ID，不进入模型。</p><p>取消、延期、停售、已结束比赛直接 NO_BET。</p><p>最高 EV 未超过动态阈值，默认 NO_BET。</p><p>模型分歧过高，禁止推荐。</p><p>所有推荐必须通过 Critic，NO_BET 仓位恒为 0。</p></section><section className="panel"><div className="panel-heading"><div><h2>Critic 输出概览</h2><p>共拦截 {blocked.length} / {matches.length} 场</p></div></div><div className="summary-strip" style={{padding: 16, margin: 0}}><span>通过<b>{matches.length - blocked.length}</b></span><span>拦截<b>{blocked.length}</b></span><span>高风险<b>{matches.filter(match => match.riskLevel === "HIGH").length}</b></span></div></section></div><section className="panel" style={{marginTop: 16}}><div className="panel-heading"><div><h2>逐场 Critic 输入与输出</h2><p>officialMatchId、SP、最终概率、EV、模型分歧、数据新鲜度、状态、上下文风险</p></div></div><div className="table-scroll"><table className="data-table"><thead><tr><th>比赛</th><th>状态</th><th>数据</th><th>最高 EV</th><th>动态阈值</th><th>分歧</th><th>passed</th><th>finalAction</th><th>原因</th></tr></thead><tbody>{matches.map(match => <tr key={match.id}><td>{match.officialMatchId} · {match.homeTeam} vs {match.awayTeam}</td><td>{match.status}</td><td>{match.context?.dataFreshness ?? "STALE"}</td><td>{(Math.max(...Object.values(match.ev)) * 100).toFixed(2)}%</td><td>{(match.prediction.dynamicEvThreshold * 100).toFixed(2)}%</td><td>{(match.prediction.modelDisagreement.maxDisagreement * 100).toFixed(1)}%</td><td>{String(match.prediction.criticReport.passed)}</td><td>{match.prediction.criticReport.finalAction}</td><td>{match.prediction.criticReport.reasons.join("；") || "通过"}</td></tr>)}</tbody></table></div></section></>}</div>; }
