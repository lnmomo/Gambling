import {useMemo,useState} from "react";import PageHeader from "../components/PageHeader";import useApi from "../hooks/useApi";
type Log={id:string;time:string;operator:string;module:string;action:string;detail:string;result:string};
export default function AuditLogsPage(){const {data,loading,error}=useApi<Log[]>("/api/audit-logs",[]);const [module,setModule]=useState("");const rows=useMemo(()=>data.filter(x=>!module||x.module===module),[data,module]);return <div className="page"><PageHeader title="日志审计" subtitle="追踪真实同步、配置修改与数据源状态"/><div className="filter-bar"><select value={module} onChange={e=>setModule(e.target.value)}><option value="">全部模块</option>{[...new Set(data.map(x=>x.module))].map(x=><option key={x}>{x}</option>)}</select></div><section className="panel">{loading?<p className="empty-state">加载中...</p>:error?<p className="empty-state">{error}</p>:<div className="table-scroll"><table className="data-table"><thead><tr><th>时间</th><th>操作人</th><th>模块</th><th>动作</th><th>详情</th><th>结果</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.time).toLocaleString("zh-CN")}</td><td>{x.operator}</td><td>{x.module}</td><td>{x.action}</td><td>{x.detail}</td><td>{x.result}</td></tr>)}</tbody></table></div>}</section></div>}
