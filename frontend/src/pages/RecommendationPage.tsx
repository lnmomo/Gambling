import {useMemo, useState} from "react";
import {Link} from "react-router-dom";
import FilterBar from "../components/FilterBar";
import ModelVersionBadge from "../components/ModelVersionBadge";
import PageHeader from "../components/PageHeader";
import RecommendationStatusBadge from "../components/RecommendationStatusBadge";
import useOfficialMatches from "../hooks/useOfficialMatches";
import type {RecommendationLifecycleStatus} from "../types";
const lifecycle = (recommendation:string, value?:RecommendationLifecycleStatus):RecommendationLifecycleStatus => value ?? (recommendation === "NO_BET" ? "NO_BET" : "ACTIVE");
export default function RecommendationPage(){const {matches,loading,error}=useOfficialMatches(),[showAll,setShowAll]=useState(false);const filtered=useMemo(()=>matches.filter(match=>showAll||lifecycle(match.recommendation,match.prediction.lifecycleStatus)==="ACTIVE"),[matches,showAll]);return <div className="page"><PageHeader title="推荐中心" subtitle="默认只显示 ACTIVE 推荐；过期、降级、撤回和已关闭项目保留审计记录。"/><section className="summary-strip"><span>全部比赛<b>{matches.length}</b></span><span>ACTIVE<b>{matches.filter(match=>lifecycle(match.recommendation,match.prediction.lifecycleStatus)==="ACTIVE").length}</b></span><span>非活动<b>{matches.filter(match=>lifecycle(match.recommendation,match.prediction.lifecycleStatus)!=="ACTIVE").length}</b></span></section><section className="panel"><FilterBar><label><input type="checkbox" checked={showAll} onChange={event=>setShowAll(event.target.checked)}/> 显示全部生命周期</label></FilterBar>{loading?<p className="empty-state">正在计算...</p>:error?<p className="empty-state">{error}</p>:<div className="table-scroll"><table className="data-table"><thead><tr><th>比赛</th><th>概率来源</th><th>最终概率</th><th>EV</th><th>推荐</th><th>生命周期</th><th>操作</th></tr></thead><tbody>{filtered.map(match=><tr key={match.id}><td><b>{match.homeTeam} vs {match.awayTeam}</b><br/><code>{match.officialMatchId}</code></td><td><ModelVersionBadge source={match.prediction.probabilitySource} version={match.prediction.modelVersion}/></td><td>{Object.values(match.prediction.finalProbability).map(value=>`${(value*100).toFixed(1)}%`).join(" / ")}</td><td>{Object.values(match.prediction.ev).map(value=>`${(value*100).toFixed(1)}%`).join(" / ")}</td><td>{match.recommendation}</td><td><RecommendationStatusBadge status={lifecycle(match.recommendation,match.prediction.lifecycleStatus)}/></td><td><Link className="table-action" to={`/matches/${match.id}`}>查看详情</Link></td></tr>)}</tbody></table></div>}</section></div>}
