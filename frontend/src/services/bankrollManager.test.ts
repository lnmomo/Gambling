import {describe,expect,it} from "vitest";
import {calculateStakeRecommendation,createDefaultBankrollConfig} from "../algorithm/bankrollManager";
import {calculatePortfolioExposure} from "../algorithm/portfolioRisk";
const prediction=(recommendation:"HOME"|"NO_BET"="HOME")=>({matchId:"m",officialMatchId:"o",recommendation,officialSp:{home:2,draw:3,away:4},finalProbability:{home:.6,draw:.2,away:.2},ev:{home:.2,draw:-.4,away:-.2},externalMarketQuality:{qualityLevel:"HIGH"},riskLevel:"LOW",confidence:"A",modelDisagreement:{level:"LOW",homeDisagreement:0,drawDisagreement:0,awayDisagreement:0,maxDisagreement:0}} as any);
describe("bankroll manager",()=>{it("uses default 1/4 Kelly and single bet cap",()=>{const config=createDefaultBankrollConfig(),exposure=calculatePortfolioExposure([],config.currentBankroll);const stake=calculateStakeRecommendation(prediction(),config,exposure);expect(config.kellyFraction).toBe(.25);expect(stake.finalStake).toBeLessThanOrEqual(1);expect(stake.stakePctOfBankroll).toBeLessThanOrEqual(.01)});it("returns NO_BET stake for no bet",()=>{const config=createDefaultBankrollConfig();expect(calculateStakeRecommendation(prediction("NO_BET"),config,calculatePortfolioExposure([],100)).status).toBe("NO_BET")});});
