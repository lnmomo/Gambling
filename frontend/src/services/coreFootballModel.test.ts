import{describe,it,expect}from"vitest";import{calculateEnhancedPureModel}from"../algorithm/coreFootballModel";const rows=Array.from({length:30},(_,i)=>({id:String(i),league:"L",homeTeam:i%2?"B":"A",awayTeam:i%2?"A":"B",homeGoals:2,awayGoals:1,playedAt:new Date(2024,0,i+1).toISOString()}));describe("enhanced pure model",()=>{it("returns normalized breakdown and fallback",()=>{const r=calculateEnhancedPureModel({league:"L",homeTeam:"A",awayTeam:"B",kickoffTime:"2025-01-01"},rows,{});expect(r.pureModelProbability.home+r.pureModelProbability.draw+r.pureModelProbability.away).toBeCloseTo(1);expect(r.xgPoissonProbability).toBeUndefined();expect(["LOW","MEDIUM","HIGH"]).toContain(r.reliability);expect([r.lambdaHome,r.lambdaAway]).toSatisfy(xs=>xs.every(Number.isFinite))});it("lineup changes lambda",()=>{const base=calculateEnhancedPureModel({league:"L",homeTeam:"A",awayTeam:"B",kickoffTime:"2025-01-01"},rows,{}),injured=calculateEnhancedPureModel({league:"L",homeTeam:"A",awayTeam:"B",kickoffTime:"2025-01-01"},rows,{playerEvents:[{id:"x",team:"HOME",type:"INJURY",position:"FWD",importance:"CORE",confidence:1}]});expect(injured.lambdaHome).toBeLessThan(base.lambdaHome)})});
