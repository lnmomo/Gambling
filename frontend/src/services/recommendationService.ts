import type {LiveRecalculationTrigger, MatchPrediction, MatchStatus, RecommendationLifecycleEvent} from "../types";
import {createLifecycleEvent, determineRecommendationLifecycle} from "../algorithm/recommendationLifecycle";
import {appendAuditLog} from "../algorithm/auditLog";
export function updateRecommendationLifecycle(matchId:string, officialMatchId:string, status:MatchStatus, current:MatchPrediction, previous?:MatchPrediction, trigger?:LiveRecalculationTrigger):RecommendationLifecycleEvent { const decision=determineRecommendationLifecycle(previous,current,status); const event=createLifecycleEvent(matchId,officialMatchId,current,previous,decision.status,decision.reason,trigger); const audit=appendAuditLog({entityType:"RECOMMENDATION",entityId:matchId,action:decision.status==="WITHDRAWN"?"RECOMMENDATION_WITHDRAWN":previous?"RECOMMENDATION_UPDATED":"RECOMMENDATION_CREATED",summary:decision.reason,before:previous,after:{...current,lifecycleStatus:decision.status},trigger,severity:decision.status==="ACTIVE"?"INFO":"WARNING",actor:"SYSTEM"}); return {...event,auditLogId:audit.id}; }
