import {describe, expect, it} from "vitest";
import {createExternalOddsSnapshot, createOfficialSpSnapshot, inferSnapshotType} from "../algorithm/snapshotEngine";
const match = {id:"m1", officialMatchId:"J001", kickoffTime:"2026-06-15T13:00:00Z", status:"NOT_STARTED" as const};
describe("snapshot engine", () => { it("normalizes official SP and marks opening snapshots", () => { const row = createOfficialSpSnapshot(match, {home:2, draw:3.5, away:4}, "2026-06-15T08:00:00Z", "OPENING"); expect(row.isValid).toBe(true); expect(Object.values(row.marketProbability).reduce((a,b)=>a+b,0)).toBeCloseTo(1); }); it("builds an external consensus snapshot", () => { const row = createExternalOddsSnapshot(match, [{bookmaker:"A",market:"H2H",odds:{home:2.1,draw:3.4,away:3.8},lastUpdate:"2026-06-15T08:00:00Z"}], {home:.45,draw:.3,away:.25}, "2026-06-15T08:00:00Z"); expect(row.externalMarketQuality.bookmakerCount).toBe(1); }); it("recognizes pre-match snapshots", () => expect(inferSnapshotType(match, "2026-06-15T12:45:00Z")).toBe("PRE_MATCH")); });
