import {describe,expect,it} from "vitest";
import {applyStakeAdjustmentFactors,calculateStakeAdjustmentFactors} from "../algorithm/stakingModel";
import type {MatchPrediction} from "../types";
const prediction=(overrides:Partial<MatchPrediction>={}):MatchPrediction=>({matchId:"m",officialMatchId:"o",recommendation:"HOME",officialSp:{home:2,draw:3,away:4},finalProbability:{home:.55,draw:.25,away:.2},ev:{home:.1,draw:-.25,away:-.2},externalMarketQuality:{qualityLevel:"LOW"} as MatchPrediction["externalMarketQuality"],riskLevel:"HIGH",confidence:"C",modelDisagreement:{level:"LOW",homeDisagreement:0,drawDisagreement:0,awayDisagreement:0,maxDisagreement:0}} as MatchPrediction);
describe("staking model",()=>{it("reduces stake for low external quality and high risk",()=>{const factors=calculateStakeAdjustmentFactors(prediction());expect(factors.some(f=>f.factor<1&&f.name==="external_market_quality")).toBe(true);expect(factors.some(f=>f.factor<1&&f.name==="risk_level")).toBe(true)});it("sets stake to zero when drawdown is paused",()=>{const factors=calculateStakeAdjustmentFactors(prediction(),{drawdownState:{riskMode:"PAUSED",stakeMultiplier:0} as any});expect(applyStakeAdjustmentFactors(10,factors)).toBe(0)});});
